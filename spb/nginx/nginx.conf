upstream spb {
    server spb_web:8088;
}

server {

    listen 8089;
    client_max_body_size 20M;

    location / {
        proxy_pass http://spb_web;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }

    location /static/ {
        alias /home/app/web/staticfiles/;
    }

    location /media/ {
        alias /home/app/web/mediafiles/;
    }

}
